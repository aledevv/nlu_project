# NLU project

This repo contains the code of the Natural Language Understanding project course @UniTrento consisiting in 2 parts: 
1. optimizing a LSTM-based N-Gram model (LM)
2. optimizing a LSTM-based and finetune a BERT model for intent classification and slot filling (NLU)
